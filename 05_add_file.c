#include <stdio.h>

int main(){
    int a = 10;
    if (a < 5) {
        printf("a < 5\n");
    }
    else {
        printf("a > 5\n");
    }
    printf("05_add_file.c");
    printf("i need to rebase the top commit into one line\n");

    return 0;
}
